'use strict';

var StateLayer = require('../../src/js/view/stateLayer');

// function createGridMock() {
//     return {
//         dimensionModel: new Backbone.Model()
//     }
// }
describe('view.layer.empty', function() {
    // it('initialize()', function() {
    //     var gridMock = createGridMock(),
    //         layer = new StateLayer({
    //             grid: gridMock
    //         });
    //
    //     layer.render();
    //     expect(layer.$el.html()).toEqual('데이터가 존재하지 않습니다.');
    // });
    //
    // it('initialize()', function() {
    //     var gridMock = createGridMock(),
    //         emptyMessage = 'Empty!',
    //         layer;
    //
    //     gridMock.emptyMessage = emptyMessage;
    //     layer = new StateLayer({
    //         grid: gridMock
    //     });
    //     layer.render();
    //     expect(layer.$el.html()).toEqual(emptyMessage);
    // })
});
